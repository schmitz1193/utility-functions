module.exports = function(length) {
var array = [];
 for (i = 0; i<length; i++) {
   array.push(i);
  };
  return array;
};

  // if (length) {
  // return new Array(length)
  // } else {
  // return[]
  // }
//
// var array = [];
// array[length] = '';
//need to handle the undefined

